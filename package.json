{
  "name": "acecapital",
  "version": "1.0.0",
  "description": "AceCapital Investor Platform",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "dependencies": {
    "nodemailer": "^8.0.7"
  },
  "scripts": {
    "install:all": "npm install && npm --prefix backend install && npm --prefix frontend install",
    "build": "npm --prefix frontend run build",
    "start": "npm --prefix backend start"
  }
}
